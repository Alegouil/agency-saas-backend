import React from "react";
import ReactDOM from "react-dom/client";
import AgencySaaS from "./AgencySaaS.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AgencySaaS />
  </React.StrictMode>
);
